# 프로젝트 개발 태스크 리스트 (일자별 기록)

## 📅 2026-06-19 (금)
- [x] **자동차보험 계산기 상단 단계 표시줄(stepper) 모바일 반응형 레이아웃 최적화**:
  - 모바일 해상도에서 단계 표시 인디케이터가 겹치거나 잘리지 않도록 CSS 미디어 쿼리 조정 및 플렉스 랩 대응.
- [x] **자동차 사고 과실비율 페이지 모바일 반응형 레이아웃 개선**:
  - 모바일 환경에서 기본/수정 과실 테이블 및 시뮬레이터 배치를 수직 구조로 재배열하여 가독성 강화.
  - 상단 상황 이미지/동영상과 하단 상세 요소 선택 탭 간의 뷰포트 크기 최적화.
- [x] **맥브라이드 장해표 검색 팝업 개선**:
  - 기본율, 옥외, 옥내의 텍스트 영역을 각각 호버 애니메이션이 들어간 개별 선택형 클릭 버튼으로 분리하여 조작성 및 편의성 극대화.

## 📅 2026-06-20 (토)
- [x] **자동차보험 계산기 단계 2 입력 필드 재배치 및 레이블 수정**:
  - `#auto-hosp-period-group` (입원기간 입력) 필드를 사고발생일과 과실비율 사이로 이동.
  - 대표 레이블명을 '입원 기간 입력 방식'에서 **'입원기간'**으로 간소화.
- [x] **반기별 도시일용노임 일자별 차등 연산 엔진 전면 리팩토링**:
  - 기존 단일 노임 일괄 적용 방식을 탈피하여, 입원/간병/장해 전 기간에 걸쳐 날짜 루프를 돌며 역사적 도시일용노임을 정밀 차등 누적 연산.
  - 월 소득이 도시일용노임 하한선 미만일 경우 최저 보장 금액으로 자동 보정 (`Math.max`).
  - 결과 상세 내역에 기간별 노임, 호프만 계수, 분할 금액을 개별 라인으로 상세 렌더링.
- [x] **입력 필드 포커스 이동 및 Enter 키 핸들러 추가**:
  - 생년월일/사고발생일 입력 시 다음 필드로의 포커스 흐름 보완.
  - 입원일수 직접 입력창(`auto-hosp-days-direct`)에서 Enter 키 입력 시 과실비율 입력창으로 자동 포커스 이동 처리.
- [x] **결과 화면 텍스트 조건부 분기 및 문구 수정**:
  - 추가된 장해 개수가 1개인 경우 `(중복장해)` 문구 대신 **`(장해)`**로 조건부 출력 처리.
  - 과실상계 적용 비율 요약 설명 문구에서 `+ 휴업손해` 문구 제거 (`[위자료 + 상실수익액]`).
  - 계산 요약 리스트 내 입원 기간 표시 형식을 **`입원 기간 : 30일`** 포맷으로 간결화.
- [x] **결과 내역 테이블 금액 열 줄바꿈(밀림) 방지**:
  - `styles.css` 내 계산 금액 열에 `white-space: nowrap !important` 속성을 반영하여 숫자와 '원' 단위가 줄바꿈되지 않도록 고정.
  - `index.html` 내 계산 금액 헤더 너비를 `min-width: 140px`로 확보.
- [x] **전체 계산 단계(Step 2, Step 3, Step 4) 흰색 폼 박스 폭 통일**:
  - 기존 600px/700px 혼용을 피하고 일관성 있게 **`max-width: 900px`**로 확대 통일하여 전면 여유 공간 확보.
- [x] **결과 내역 표 정렬 규칙 통일 및 들여쓰기 적용**:
  - 표 제목 헤더를 **가운데 정렬(`text-align: center;`)**로, 행 내용 상세는 **왼쪽 정렬(`text-align: left;`)**로 변경.
  - 상실수익액 산출 내역 내 줄바꿈되는 둘째 줄 시작 지점에 **`&nbsp;&nbsp;&nbsp;&nbsp;` (4칸 들여쓰기)** 적용하여 기호와 정렬 정밀 조정.
- [x] **프로젝트 개발 통합 기록 문서 작성 및 워크스페이스 통합**:
  - `development_log.md`를 루트 폴더에 생성하여 기획 단계부터의 상세 히스토리 영구 기입.
- [x] **자동 Obsidian 동기화 연동 구축**:
  - `sync_obsidian.py`를 제작하여 `반중력프로젝트/laika` 폴더 하위에 `개발_기록_업데이트_노트.md`, `변경_보고서_워크스루.md`, `작업_태스크_리스트.md` 3대 보고서를 자동 복사/업로드 및 동기화 구현.
- [x] **62세 이상 고령 피해자 연산 보원 구현**:
  - [/] `script.js` 내 `window.getGadongMonths` 리팩토링 (62세 이상 취업가능월수 테이블 하드코딩 매핑)
  - [ ] `script.js` 내 `window.getAutoManAgeAtDate` 날짜별 나이 계산 헬퍼 함수 구현
  - [ ] 후유장해 상세계산 루프 내 만 65세 정년 기준 소득 이중 차등 적용 로직 반영
  - [ ] 후유장해 단순계산 루프 내 만 65세 정년 기준 소득 이중 차등 적용 로직 반영
  - [ ] 사망 계산 루프 내 만 65세 정년 기준 소득 이중 차등 적용 로직 반영
- [ ] **GitHub 원격 저장소 최신화**:
  - 모든 변경 코드 및 마크다운 문서를 GitHub 원격 저장소에 최종 푸시 및 동기화 완료.
